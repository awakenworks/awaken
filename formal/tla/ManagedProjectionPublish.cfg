CONSTANTS
    Writers = {"writer-a", "writer-b"}
    \* Three representative independent clocks bound the state space while
    \* retaining the component-wise partial order. The Rust fence applies the
    \* same rule to every child Thread, lifecycle cursor, and budget generation.
    Components = {"session", "root-thread", "other-source"}
    MaxSourceVersion = 2
    MaxCacheRevision = 4
    NoWriter = "no-writer"

INIT Init
NEXT Next

INVARIANTS
    Safety
