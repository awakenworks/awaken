\* Cause/effect witness E1. Causes: committed source request, open Session
\* activity, durable target dispatch admission, and parent Runtime receipt commit
\* before target execution. Effects: the target message and boundary commit,
\* only that boundary settles activity, and dispatch then settles exactly once.
SPECIFICATION EarlyReceiptSpec

CONSTANTS
    Owners = {worker_a}
    NoOwner = no_owner
    MessageActivity = agent_message
    MaxEpoch = 1

INVARIANTS Safety
PROPERTIES EarlyReceiptJourneyCompletes

CHECK_DEADLOCK FALSE
