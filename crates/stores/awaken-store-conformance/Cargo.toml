[package]
name = "awaken-store-conformance"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
awaken-agent-contract.workspace = true
serde_json.workspace = true
# `join!` drives the concurrent-append case's two commits on one task, portably
# across backends (no per-backend spawn/Clone/Send bounds).
tokio.workspace = true

[lints]
workspace = true
