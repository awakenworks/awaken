CREATE TABLE {prefix}_item (work_id             TEXT PRIMARY KEY, seq                 BIGINT NOT NULL, environment_id      TEXT NOT NULL, data_type           TEXT NOT NULL, data_id             TEXT NOT NULL, metadata_json       TEXT NOT NULL, state               TEXT NOT NULL, acknowledged_at     TEXT, latest_heartbeat_at TEXT, started_at          TEXT, stop_requested_at   TEXT, stopped_at          TEXT)
