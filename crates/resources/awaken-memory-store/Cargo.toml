[package]
name = "awaken-memory-store"
version.workspace = true
edition.workspace = true
license.workspace = true

# Durable memory persistence for the resources plane. Deliberately std-only: a
# leaf like awaken-file-store, it names no runtime/host/provider type so the host
# can back the ADR-0038 MemoryStore family with it while the runtime stays unaware.

[lints]
workspace = true
