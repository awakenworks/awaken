[package]
name = "awaken-credential"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Host-side credential vocabulary shared by Awaken's outbound wire clients"

[dependencies]
async-trait.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["rt", "macros"] }

[lints]
workspace = true
