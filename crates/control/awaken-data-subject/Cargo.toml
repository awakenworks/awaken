[package]
name = "awaken-data-subject"
version.workspace = true
edition.workspace = true
license.workspace = true

# The data-subject aggregate (ADR-0050): the party a run is attributed to, its
# per-purpose consent grants, and the erasure/consent-ceiling resolver. A
# control/compliance-plane store (peer of awaken-credential-vault); Org-scoped.

[dependencies]
awaken-agent-contract.workspace = true
awaken-runtime-contract.workspace = true
async-trait.workspace = true
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
rusqlite = { workspace = true }
# `spawn_blocking` for the sqlite adapter's off-thread connection work.
tokio = { workspace = true, features = ["rt"] }
# Feature `postgres`: network-DB DataSubjectRepo + CapturedContentStore adapters
# over separately owned Control and Coordinator scopes (ADR-0050). The shared
# crate location is transitional; the migration bundles do not share a prefix.
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "postgres", "json"], optional = true }
awaken-scoped-migration = { workspace = true, features = ["postgres"] }
awaken-scoped-migration-sqlite.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
tempfile = "3"
# The live Postgres conformance test (feature `postgres`) isolates each suite in
# its own schema via `SET search_path`.
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "postgres"] }

[features]
default = []
# Postgres-backed DataSubjectRepo + CapturedContentStore over the same per-owner
# portable bundles as SQLite (ADR-0050 D7, Org-partitioned).
postgres = ["dep:sqlx"]

[lints]
workspace = true
