# Archived scripts

One-shot or retired scripts kept for reference. Not part of any active
workflow. Do **not** rerun without auditing what they touch.

## Contents

- `migrate-mdbook.mjs` — one-shot mdBook → Starlight migration. Source
  directory (`docs/book/src`) was removed after the cutover, so rerunning
  this script as-is will fail. Kept as the historical record of how the
  Starlight files in `apps/www/src/content/docs/` were generated.
