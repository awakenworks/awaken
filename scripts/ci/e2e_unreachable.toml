version = 1

# These ranges are not blanket source exclusions. The checker waives only lines
# that are changed, executable, and still unhit; any E2E hit inside a range stays
# in the numerator. Every range names the lower-level evidence that owns it.

[[waiver]]
path = "crates/worker/awaken-sandbox-local/src/namespace.rs"
ranges = ["44-53", "203-212", "215-217", "219-222", "230-232", "234-237", "239-244", "246-251", "253", "260-269", "272-279", "382-385", "521", "527-530", "534", "587", "765", "783", "788-796", "798", "800-810", "813-814", "827", "844", "875", "902"]
reason = "macOS Seatbelt rendering, unsupported mount kinds, and operating-system failure arms are platform adapter boundaries that the Linux API harness cannot select."
evidence = "cargo test -p awaken-sandbox-local; e2e/sandbox_backend_e2e.ts"

[[waiver]]
path = "crates/control/awaken-admin-config-api/src/sqlite.rs"
ranges = ["188-190", "192-195", "210-211", "213", "230-233", "235-236", "240", "244", "249-250", "252-259", "261", "268", "271-277", "282", "285", "287-289", "293-295", "297", "302"]
reason = "SQLite lock poisoning, codec corruption, and legacy webhook outbox adapter failures require direct repository fault injection below a valid management request."
evidence = "cargo test -p awaken-admin-config-api; e2e/control_plane_sqlite_e2e.ts"

[[waiver]]
path = "crates/server/awaken-server/src/legacy_resource_migration.rs"
ranges = ["30", "62", "67-68", "78-79", "85-86", "89-91", "97-98", "100-101", "107", "130-132", "161-163", "165-170", "174-180", "182"]
reason = "Malformed legacy rows, migration conflicts, and backend failures are startup-only compatibility failures and are not selectable through a served API."
evidence = "cargo test -p awaken-server; e2e/resource_legacy_upgrade_e2e.ts"

[[waiver]]
path = "crates/control/awaken-model-catalog/src/sqlite.rs"
ranges = ["240", "242-255", "257-266", "268-271"]
reason = "SQLite transaction, codec, and reconciliation failures are repository adapter faults below the provider model directory API."
evidence = "cargo test -p awaken-model-catalog; e2e/provider_model_directory_e2e.ts"

[[waiver]]
path = "crates/bin/awaken-cli/src/console.rs"
ranges = ["38-40", "43", "82-88", "90-91", "93", "95-101", "103-106", "108"]
reason = "Missing checkout assets and package-manager spawn failures belong to the local console build launcher rather than the running management API."
evidence = "cargo test -p awaken-cli; e2e/console_start_e2e.mjs"

[[waiver]]
path = "crates/server/awaken-runtime-host/src/host/session.rs"
ranges = ["803-820"]
reason = "Discarded-environment ABA and internal recovery races require synthetic lifecycle interleavings below an accepted Session command."
evidence = "cargo test -p awaken-runtime-host; e2e/session_environment_faults_e2e.ts"

[[waiver]]
path = "crates/server/awaken-webhook-managed/src/lib.rs"
ranges = ["211-212", "214", "225", "481-486", "502-507", "538-541", "565"]
reason = "Legacy webhook store, outbox, and secret-sealing fault arms require injected adapter failures rather than semantic webhook requests."
evidence = "cargo test -p awaken-webhook-managed; e2e/webhook_e2e.ts"

[[waiver]]
path = "crates/bin/awaken-cli/src/lib.rs"
ranges = ["373-378", "848-853", "869", "888", "945"]
reason = "Startup migration logging, test-only router seams, and alternate internal composition constructors are not exposed as management API behavior."
evidence = "cargo test -p awaken-cli; e2e/console_start_e2e.mjs; e2e/control_plane_sqlite_e2e.ts"

[[waiver]]
path = "crates/runtime/awaken-ext-memory/src/extraction.rs"
ranges = ["33-51"]
reason = "The host-executor snapshot constructor is an embedded and test persistence helper; hosted execution creates snapshots from published provisioning instead."
evidence = "cargo test -p awaken-ext-memory; e2e/memory_extraction_stage_recovery_e2e.ts"

[[waiver]]
path = "crates/control/awaken-admin-config-api/src/postgres.rs"
ranges = ["314-317", "324", "329", "334-336", "343", "348-349", "354", "359", "363"]
reason = "Legacy webhook outbox operations are retained only behind the repository adapter while the production lifecycle uses the Session outbox."
evidence = "cargo test -p awaken-admin-config-api; e2e/control_plane_postgres_e2e.ts"

[[waiver]]
path = "crates/control/awaken-credential-vault/src/lib.rs"
ranges = ["510", "537-539", "546-548", "620-622", "646-648"]
reason = "Convenience creation plus impossible secret/reference combinations and control-plane materialization of worker-local credentials are rejected at earlier boundaries."
evidence = "cargo test -p awaken-credential-vault; e2e/control_plane_postgres_e2e.ts"

[[waiver]]
path = "crates/control/awaken-config-service/src/publication.rs"
ranges = ["62-64", "70-72", "77-79", "82-84"]
reason = "Duplicate, reordered, or mismatched output from a publication resolver violates the in-process resolver port contract, not a client-selectable publication input."
evidence = "cargo test -p awaken-config-service; e2e/publication_truth_e2e.ts"

[[waiver]]
path = "crates/server/awaken-runtime-host/src/acp_provision.rs"
ranges = ["41-44", "46-48", "52-54"]
reason = "Candidates outside the publication, unsupported provisioning, and incomplete endpoints are fail-closed guards after publication validation."
evidence = "cargo test -p awaken-runtime-host; e2e/acp_projected_local_e2e.ts; e2e/acp_projected_container_e2e.ts"

[[waiver]]
path = "crates/control/awaken-config-store/src/compile.rs"
ranges = ["66", "229-230", "232-235", "244-247"]
reason = "Resolver cardinality, ordering, and candidate identity mismatches are defensive checks on a trusted in-process resolver implementation."
evidence = "cargo test -p awaken-config-store; e2e/publication_truth_e2e.ts"

[[waiver]]
path = "crates/server/awaken-server/src/model_resolver.rs"
ranges = ["44", "47", "185-189", "197-201", "221", "245-249", "274-276"]
reason = "Test constructors, corrupted pinned catalog revisions, impossible canonical binding disagreements, and injected repository failures sit below valid authoring inputs."
evidence = "cargo test -p awaken-server; e2e/provider_model_directory_e2e.ts; e2e/publication_truth_e2e.ts"

[[waiver]]
path = "crates/control/awaken-admin-config-api/src/router.rs"
ranges = ["322-330", "507-514"]
reason = "Injected configuration-repository failure and absence of the production discovery adapter are composition faults, not management API choices."
evidence = "cargo test -p awaken-admin-config-api; e2e/control_plane_postgres_e2e.ts"

[[waiver]]
path = "crates/control/awaken-admin-config-api/src/postgres_resource_catalog.rs"
ranges = ["355", "394", "404", "475", "490", "500"]
reason = "PostgreSQL aggregate codec and storage failures require direct adapter fault injection below the resource catalog application service."
evidence = "cargo test -p awaken-admin-config-api; e2e/control_plane_postgres_e2e.ts"

[[waiver]]
path = "crates/control/awaken-admin-config-api/src/sqlite_resource_catalog.rs"
ranges = ["334", "371", "450", "463", "473"]
reason = "SQLite aggregate codec and storage failures require direct adapter fault injection below the resource catalog application service."
evidence = "cargo test -p awaken-admin-config-api; e2e/control_plane_sqlite_e2e.ts"

[[waiver]]
path = "crates/control/awaken-model-catalog/src/postgres.rs"
ranges = ["207", "224", "241"]
reason = "PostgreSQL model reconciliation failures are repository adapter faults below a valid provider directory refresh request."
evidence = "cargo test -p awaken-model-catalog; e2e/provider_model_directory_e2e.ts"

[[waiver]]
path = "crates/server/awaken-runtime-host/src/credential_materializer.rs"
ranges = ["48", "77", "84-87"]
reason = "Non-provider input, revision drift, and provider mismatch are exact-pin fail-closed checks after publication and activation validation."
evidence = "cargo test -p awaken-runtime-host; e2e/credential_materialization_e2e.ts"

[[waiver]]
path = "crates/server/awaken-server/src/inference_materializer.rs"
ranges = ["121-125"]
reason = "A requested binding outside the immutable publication candidate set is an internal caller contract violation, not an accepted model override."
evidence = "cargo test -p awaken-server; e2e/model_override_e2e.ts"

[[waiver]]
path = "crates/worker/awaken-connection-plan/src/factory.rs"
ranges = ["57", "156-158"]
reason = "Unix socket connect and bind operating-system failures are transport factory boundaries outside semantic worker configuration requests."
evidence = "cargo test -p awaken-connection-plan; e2e/worker_connection_plan_e2e.ts"

[[waiver]]
path = "crates/server/awaken-runtime-host/src/lib.rs"
ranges = ["1215-1216", "1220"]
reason = "Absence of the credential vault is an invalid internal host composition rejected before any managed Session is served."
evidence = "cargo test -p awaken-runtime-host; e2e/credential_materialization_e2e.ts"

[[waiver]]
path = "crates/server/awaken-runtime-host/src/sandbox_source.rs"
ranges = ["725", "735-737", "746-747"]
reason = "Sandbox executable discovery and operating-system startup failures are deployment adapter boundaries outside a valid Session sandbox request."
evidence = "cargo test -p awaken-runtime-host; e2e/sandbox_backend_e2e.ts"

[[waiver]]
path = "crates/control/awaken-config-service/src/binding_resolver.rs"
ranges = ["48-49"]
reason = "The string conversion is an internal convenience constructor; served publication paths use the validated typed ScopeId."
evidence = "cargo test -p awaken-config-service; e2e/publication_truth_e2e.ts"
