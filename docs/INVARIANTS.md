# Invariants

These are the architecture rules that must be enforceable in code review, tests,
dependency checks, or public API checks. If a proposed implementation needs an
exception, update the owning design (ADR) first.

This file is a guardrail **index** (see [ADR-0001](adr/0001-documentation-model-and-vocabulary-alignment.md)).
Each row names the statement, the **Enforcer** (the corpus role or mechanism that
holds the rule), and the **Validation** (the test kind that proves it).

- Enforcer/Validation name this corpus's own roles and intended tests. This
  corpus is self-contained: it does not depend on any other repository to be
  understood (ADR-0001).
- The tables are the only status authority: a row under **Guardrails** is active;
  a row under **Target Guardrails** is accepted but not promoted. Do not maintain
  a second prose list of ids. `check_invariants.py` verifies that the two sets are
  disjoint and that every row names its enforcer/evidence.
- G42 and G43 remain target rows until their deployment-specific evidence is
  continuously enforced. Completed slices may be cited narrowly without claiming
  promotion of the whole guardrail.
  - **Retired**: G7 (`ExecutionBackend`/`BackendProfile`). That god-seam stays
    retired. Remote tool execution returned in ADR-0044 as the narrow `ToolExecutor`
    port with a concrete driver and tests (G33), not as `ExecutionBackend`; the
    runtime still executes tools in-process by default (ADR-0007). The row is kept
    as a tombstone so G8–G34 references stay stable.
- The rationale (problem, decision, consequence) behind these guardrails is owned
  by [key-design-decisions.md](design/key-design-decisions.md) (D1–D22); this
  index records only the enforceable statement, enforcer, and validation, and
  does not restate that rationale.
- The `goal` worktree is a non-authoritative reference implementation for
  patterns only; it is never cited here as the enforcer.

## Guardrails

| ID | Statement | Enforcer | Validation |
|---|---|---|---|
| G1 | Durable runtime writes go through `CommitCoordinator`; protocol projections are after-commit. | `CommitCoordinator` stages durable writes; `ThreadCommit` validates the commit plan; projections derive after commit | staged-commit tests; projection-ordering tests |
| G2 | Runtime core names only runtime-domain types and the approved runtime/server port. | runtime-core public API surface plus the single gated runtime/server port | metadata-derived `scripts/ci/check_crate_boundaries.py`; public API snapshots (`scripts/ci/check_public_api.sh`) |
| G3 | Config-to-runtime input is serializable, immutable snapshot data. No live registry, `Arc<dyn ...>`, ambient request scope, plaintext secret, or product DTO crosses into runtime core. A secret-free resource-owner pin may be part of fingerprinted access data so execution can reject cross-scope materialization without performing authorization. | `ExecutableAgentSnapshot` (`ResolvedSpec`, metadata, fingerprint) as the config edge | serde boundary assertions (`tests/serde_boundary.rs`, via `cargo check --all-targets`); no-secret serialization and fingerprint-mismatch tests |
| G4 | Runtime builds live execution objects from an immutable executable snapshot and fails closed on empty or internally inconsistent snapshot fingerprints. | `PublishedAgentSnapshotSource` supplies immutable snapshots; `StaticPublishedAgentSnapshots::try_new` and `RunResolver` reject inconsistent fingerprints | resolver and snapshot-source fingerprint mismatch tests in `awaken-runtime-contract`; publication metadata/fingerprint tests in `awaken-config-service` |
| G5 | `RunIngress` has exactly two delivery semantics: direct `DirectRunIngress` and durable `DurableRunIngress`; durable-only operations fail closed on direct ingress. An accepted durable cancellation is retained until a fenced terminal commit and settlement; terminal control cannot be blocked by execution placement or sandbox availability. | `RunIngress` with `DirectRunIngress` / `DurableRunIngress`; `cancel_requested` is claimed through the ordinary dispatch epoch, bypasses execution placement ranking, resolves a control-only worker, and live signalling occurs only after intent persistence | `RunIngress` capability tests; shared memory/SQLite/PostgreSQL cancellation store spec (including incompatible worker/policy); host invalid-sandbox control-resolution test; intent-before-commit restart and commit-before-settle crash tests |
| G6 | Durable ingress behavior is additive over runtime control; durable ingress internals are not public runtime seams. | durable ingress wraps runtime control; same-source commit guard at construction | metadata-derived context/layer direction; the worker shares one commit source for write and read by construction; durable submit/resume/recovery tests drive the same `RunExecutor`/`Runtime::resume` as direct |
| G7 | _(retired, ADR-0007)_ The runtime executes tools in-process; there is no `ExecutionBackend`/`BackendProfile` execution-placement seam. Remote/out-of-process agent execution is out of scope until a future ADR reintroduces it with a concrete driver and tests. | — | — |
| G8 | Capability configuration is segmented: descriptors are pinned/fingerprinted, execution behavior is owned by the runtime/extension and invoked in-process by id, operator policy is mutable, secrets are opaque refs, session data is runtime fact state. The operator-policy segment allows/denies a plugin by its declared `CapabilityBound` (especially `tool_gate` / `transforms` / namespace), the bound being the dry-run-`resolve`-derived projection on `PluginCapability`. | pinned `ToolDescriptor` / `ResolvedSpec` segments; behavior invoked by id; mutable operator policy keyed on `CapabilityBound` projection; opaque secret refs; session = fact state | config materialization tests; no-secret serialization tests; bound-projection on `PluginCapability`; `scripts/ci/check_crate_boundaries.py` blocks concrete builtin tool ids/symbols in neutral crates |
| G9 | Selection, capability compatibility, and health probes are never authorization; their result types carry no grant. | selection/compatibility/health result types carry no grant; `ToolGateHook` + permission policy is the only grant path | type/API checks; explicit-authorization permission tests |
| G10 | Public protocol names live only in adapters/anti-corruption layers. Runtime errors and events use neutral names. | neutral runtime error/event names; public names only in protocol adapters | grep/deny-list over runtime crates; public API snapshots (`scripts/ci/check_public_api.sh`) |
| G11 | Outcome semantics and the cross-Run controller live in `awaken-ext-goal`; product adapters only translate public vocabulary. Runtime Core records neutral Run/Thread facts and opaque continuation detail but does not interpret Outcome. | `awaken-ext-goal` controller/state codec over `RunExecutor`, `CommittedThreadView`, and `CommitCoordinator`; adapters project public results | extension boundary tests; stable-id recovery; Runtime vocabulary deny-list |
| G12 | Committed-terminal extension reactions are at-least-once observers: they run only after `RunState::Ended` commits, cannot alter `RunResult`, and use stable intents/receipts for recovery and duplicate suppression. Awaiting is not terminal. | `RunTerminalObserver` plus extension-owned intent/receipt; terminal redelivery from committed truth | all-EndCause delivery tests; Awaiting negative test; crash-gap and duplicate-intent tests |
| G13 | Store truth is single-source within a commit; outbox/protocol replay entries are committed with the authoritative state or derived after commit. | `CommitCoordinator` is the single durable write boundary; outbox/replay committed with state or derived after | transactional store tests; `ThreadCommit` serde boundary assertions; no split-store dual-write review |
| G14 | Every development-ready design names bounded context, model element, port/repository, owner, guardrail, enforcer, and first vertical slice. | ADR template (ADR-0001 D1/D2) plus this index | docs review checklist; `check_adr` and `check_invariants` hooks |
| G15 | Runtime protocol/specification material, SDK-facing schemas, examples, and conformance tests remain Apache-2.0 unless a design explicitly moves them out of the runtime boundary; code packages may carry their own package or file license metadata. | `lefthook.yml` SPDX/license hooks; `LICENSE-APACHE` / `LICENSE-MIT` plus per-package `license` metadata | license file checks; SPDX header checks |
| G16 | Neutral runtime, protocol, config, and ordinary extension code does not use product-hosting vocabulary such as `managed`; those names are restricted to product adapters or explicit boundary mapping docs. | `lefthook.yml` vocabulary deny-list over neutral crates | grep/deny-list checks; adapter boundary review |
| G18 | Configuration publication, live control, and execution stay on separate runtime-facing ports. No API may own config authoring/publication, active-run steering, loop execution, durable commit, and public projection as one controller. | separate seams: `RunResolver` / `LiveRunControl` / `RunExecutor` / `CommitCoordinatorSource`; live-control seam now enforced by `LiveRunControlService` (cancel/wake fail-closed by correlation-id, separate from `RunIngress` submission) | metadata-derived context/layer direction; `LiveRunControlService` fail-closed tests (`awaken-run-ingress`: cancel NotFound, wake NoSubscriber on unknown id) |
| G19 | Public protocol adapters own public DTOs, event names, headers, replay cursors, and public errors; runtime core receives only neutral activation, control, resume, and projection-source values. | protocol adapters own public DTOs; runtime gets neutral activation/control/resume values | adapter conformance tests; DTO leak snapshot tests |
| G20 | Executor and wait/resume channel values are plain serializable data; no live handle, `Arc<dyn ...>`, channel, or process handle crosses the executor result or resume channel boundaries. | executor result type (`RunState` + `EndCause` + `Failure`) satisfies `Serialize + DeserializeOwned`; durable live-command and wait/resume channel value types will be constrained at their landing | `assert_boundary` serde assertions in `crates/runtime/awaken-runtime-contract/tests/serde_boundary.rs` |
| G21 | Permission policy is the only authorization path for protected runtime operations; visibility, selection, health, compatibility, and successful resource realization carry no grant. | permission policy is the sole grant path; visibility/selection/health carry no grant | no-hidden-grant tests; permission decision API checks; audit commit tests |
| G22 | Model, provider, and backend bindings are selected before execution by config or adapter policy; runtime validates the selected binding and fails closed on mismatch instead of searching for replacements. | binding selected in `ResolvedSpec`; runtime validates, fails closed, never searches | binding snapshot tests; backend negotiation tests; provider-search dependency checks |
| G23 | Config publication and public runtime projection use versioned atomic boundaries: a stale config revision cannot publish, and public durable projections derive only from committed runtime truth. | `ConfigRegistry::put_publication_if_config_revision` fences publication; `CommitCoordinator` and committed-event readers fence public projection | SQLite/Postgres publication revision-conflict tests; committed projection ordering and replay tests |
| G25 | Observability, datasets, eval reports, traces, and experiments consume committed facts or normal runtime ports; they do not rewrite runtime truth or create alternate commit paths. | observability/eval read committed facts or normal ports only | dataset lineage tests; eval harness tests; trace deletion/replay tests |
| G26 | Stable errors are owned by their source domain and mapped by adapters; runtime errors remain neutral, public error schemas stay in protocol adapters, and indeterminate remote execution is explicit. | per-domain error types; adapters map public errors; explicit `Indeterminate` result | error mapping snapshots; public DTO dependency checks; indeterminate-result tests |
| G27 | Package boundaries, license metadata, import direction, vocabulary restrictions, and ownership are enforced mechanically for every new runtime-facing design or package. | mandatory Cargo `context/layer/authority` metadata plus `lefthook.yml` and `scripts/ci/` semantic hooks | metadata-derived crate-boundary hook runs in CI |
| G28 | Executable snapshots are the run/thread configuration identity at the runtime-facing boundary: runtime may accept an inline `ExecutableAgentSnapshot` or resolve an `ExecutableAgentSnapshotId`, but it must not treat `AgentId` alone as complete configuration or own config CRUD/admin workflow. | `ExecutableAgentSnapshot` inline or by id; not `AgentId` alone; no config CRUD in runtime | snapshot contract API checks; inline/by-id execution tests |
| G29 | Agent configuration loading, compilation, and durable publication stay outside runtime core. Runtime and Worker execution receive immutable `ExecutableAgentSnapshot` data and must not own config loading, version publication, or admin workflow. | `ConfigService::publish` and `ConfigRegistry` own publication; `RunActivation` and `PublishedAgentSnapshotSource` carry immutable execution data; crate dependency direction keeps config above runtime | config publication/revision/fingerprint tests; snapshot serde and resolver tests; `scripts/ci/check_crate_boundaries.py` |
| G31 | A terminal run stores one authority: the committed run `RunState` (`Awaiting \| Ended(EndCause)`). Run status, the published outcome, and the error flag are derived projections, never stored; the fault kind and explanation live in `EndCause::Error(Failure)`, not a status string or required duplicate assistant message. A natural terminal still requires committed assistant output. Every run end funnels through one commit boundary that writes the `RunState` and emits the finish event once. | single `finish` boundary writes one `RunFact { state }`; `RunState`/`EndCause`/`Failure` typing makes a second stored end-notion unrepresentable; committed-step proof accepts the typed error explanation and requires output for `NaturalEnd`; `Ended` makes awaiting/end mutually exclusive | terminal projection and committed-step proof tests (committed fault cause in fact + event; error without duplicate message; natural end without output rejected; `MaxSteps`; cancelled and state-conflict causes); public API snapshot of the `run` module (ADR-0005) |
| G32 | A Run's authoritative current state is its latest projection fact in the Thread's append-only fact log. `CommittedThreadView::run` derives any retained Run's latest record, while `latest_run` independently identifies the Thread head; neither is an authority or a second persistence path. The append fence is the committed fact count, not a cache row count. | replay reads the fact log (`replay_latest_state`); one `CommittedThreadView` projects historical Run lookup and Thread-head lookup; `CheckpointReader` only adds durable event reads; `CommitRecord.sequence` is the monotonic committed count | shared backend conformance decision table retains the historical Run when the Thread head advances; projection test (cache equals replay-from-log); waiting test (Awaiting→Ended progression retained in order, latest fact wins) (ADR-0006, ADR-0039) |
| G30 | A plugin's actual `Contributions` are a subset of its declared `CapabilityBound`, enforced fail-closed at resolve and at catalog registration; contribution identity (registration key, `ToolDescriptor` id, and bound reference) is one value by construction. Every id-bearing kind is bounded (tools, state keys, guards, effects, scheduled actions). A dynamically discovered family declares a coarse `Namespace` ceiling and submits a resolve-time tightened sub-bound from the same snapshot, enforced `contributions ⊆ tightened ⊆ static ceiling` (`within`). `CapabilityBound` is a contribution ceiling, never authorization, and shares no type with any permission decision. | `Plugin::resolve` returns `Contributions`; `ResolvedExecutionEnv` merge runs `enforce_bound` and cross-plugin uniqueness; `Contributions::tighten_bound` + `IdBound::within`; catalog boot self-check | undeclared-contribution fail-closed tests; tightened-bound-escapes-ceiling fails closed; cross-plugin clash names both plugins; register-time self-check; identity-equivalence compile check |
| G33 | Sandbox-target tool execution runs behind the SessionEnvironment-owned `ToolExecutor` (ADR-0073). Native Hand calls and ACP share the same Environment; outbound A2A owns no local Hand. No Agent/deployment/per-Run Hand placement coordinate may override the durable Worker claim. A snapshot recovery policy is a typed placement demand, and only a Worker whose manifest derives a compatible capability from the same deployment value as its SessionEnvironment may claim it. The relay remains a value-returning container adapter that links no model, commit coordinator, or store. | `SessionCtx::tool_executor` is derived only from realized/deferred `SessionEnvironment`; `PlacementRequirements`/`WorkerManifest` plus `can_claim` enforce recovery compatibility; architecture fitness rejects the retired provider/declaration symbols; `awaken-tool-relay` depends only on the neutral runtime contract | Workdir/Namespace/Container shared-state tests; recovery demand/capability claim decision table; explicit-manifest topology test; lazy first-Hand concurrency/binding tests; A2A no-Environment decision table; execution-ownership fitness; relay contract tests |
| G34 | Network topology is a serializable `ConnectionPlan` value object (ADR-0045) over the connection mechanism; it carries neither credential references nor resolved secret material, and no product-hosting vocabulary. `ChannelFactory` maps a plan to a live channel with `InProcess` as the zero-cost degenerate arm, so one brain/hand code path spans laptop to fleet. | `awaken-connection-plan` `ConnectionPlan`/`DialAddr`/`Wiring`/`DialPolicy`; product-process startup supplies transport authentication independently | `deny_unknown_fields` rejects the retired credential field; InProcess + Unix round-trip tests; `connect` fail-closed on a `Listen` plan |
| G35 | A caller-owned Run id identifies one canonical dispatch: live exact replays ignore only `traceparent`, while any different payload is rejected before placement, scheduling, or supersession eligibility. An applied durable-dispatch `Done` atomically records one replayable completion fact and permanent run-id tombstone carrying that canonical SHA-256 identity; a fenced settle records none, and completed Run ids cannot be re-admitted with an unverified or different dispatch. Historical tombstones without identity fail closed. Repair of a quiescent Awaiting row or expired Running lease requires exact committed `RunState::Ended` proof and reuses the same fenced `Done`; retry exhaustion first claims an exact epoch and commits `Ended(Indeterminate)` through that same Worker/`Done` path. Automatic dispatch maintenance never substitutes a store-only dead-letter for terminal Run truth; `DeadLetter` is explicit operator quarantine. Dispatch never accepts outcome truth, and the completion fact carries no run outcome or product vocabulary. | `RunDispatch::same_canonical_dispatch` / `canonical_fingerprint`; `DispatchQueue::settle`, `DispatchQueue::claim_for_terminal_recovery`, `DispatchQueue::claim_retry_exhausted`, and `DispatchQueue::completion_events_after`; `DispatchWorker::settle_claimed_terminal` / `terminalize_retry_exhausted` reuse one commit/observer/settle owner; unique completion row in memory/SQLite/Postgres; every enqueue and exact-new-run command checks identity before eligibility | shared dispatch conformance decision tables prove identity replay/collision rules plus retry-exhaustion eligibility, atomic claim, concurrent fencing, and `Done` across all local backends; SQLite/Postgres migration fixtures prove legacy-NULL rejection; service/pool/remote/host tests prove commit-failure retry, terminal completion, nonterminal preservation, and environment-free terminal repair (ADR-0015, ADR-0060) |
| G36 | Inference route and credential access are selected exactly once from persisted Catalog/Credential facts at scoped configuration publication and fingerprinted in `ExecutableAgentSnapshot`. Dispatch copies that snapshot; runtime may validate and materialize only a published candidate and may never enumerate catalog routes, choose another credential, weaken its injection policy, or read provider/model/credential execution facts from process environment. Process environment cannot propose or select Provider/model/credential configuration. | `CatalogModelPublicationResolver` publishes complete `ResolvedModelCandidate` values outside runtime; native and ACP share `PinnedCredentialMaterializer`; `CredentialKind::Env` creation/materialization rejects; no environment-proposal API or parallel inference-access metadata exists | publication/materialization/ACP tests; provider-env crate-boundary check; `InferenceAccessPublication.tla` |
| G37 | Resource input identity follows content lifecycle: File binds one logical `FileId` whose immutable blob mapping is Resources-owned; Agent Memory/Repository bindings carry identity only; one Session-side resolution selects their immutable configuration versions without selecting Memory entry/head versions or Git commits. Agent defaults and direct attachments use that same resolver before Session creation. The effective manifest is secret-free, and current ownership, lifecycle state, authorization, and credential revocation may still deny use. | `SessionInputResolver`, typed `InputResourceId`/`ResolvedSessionResources`/`SessionInputAttachment`, `FileApplicationService`, and immutable Memory/Repository configuration-version repositories; front-door PEP remains the authorization edge | typed-boundary and one-resolution/explicit-replacement tests; File command/idempotency tests; no-content-pin/no-secret serialization; cross-Workspace and post-revocation fail-closed tests (ADR-0063) |
| G38 | Logical resource denial precedes physical reclamation. Cleanup is idempotent and may purge only after the resource-specific reference and activation conditions hold: shared File blobs have no remaining ownership/reference, Memory has no live handle or extraction job, and Repository cleanup removes only awaken-local material, never the remote repository. The zero-reference proof and durable `(kind, resource_id)` fence are atomic with racing reference writes and contain no authorization data. | Durable `SessionResourceActivation`, `ResourcePurgeIntent`/receipt, reverse-reference index, lease/revision claim, `ResourceReclamationFence`, per-kind `ResourcePhysicalReclaimer`; PEP/PDP remains outside these ports | state-machine/property tests; in-memory/SQLite/Postgres fence conformance; shared-File race, Memory whole-store, Skill retained-pin, Repository remote-preservation tests; cross-process E2E (ADR-0063) |
| G39 | Resource state and authorization judgment are orthogonal bounded contexts. Resource ports and persistence accept a trusted Workspace coordinate plus typed operations, but never depend on IAM/authz packages or carry principal, API key/token, role, authorization policy/decision, Org, Project, or WorkUnit data. Workspace ownership, lifecycle state, CAS, references, retention, and reclamation fences remain intrinsic resource invariants rather than grants. | One front-door PEP selects the management or resource action namespace and the PDP owns judgment; resource application services and `RESOURCE_PLANE_CRATES` own state; `check_resource_authorization_isolation` rejects dependency, type, field, and schema leakage | `scripts/ci/check_crate_boundaries.py`; embedded-IAM edge E2E proves deny/allow with one authentication/authorization pass while resource stores stay identity-free (ADR-0063 D4 and 2026-08-12 amendment) |
| G40 | Native, ACP, and A2A attempts use one snapshot-selected `RunAttemptExecutor` route for fresh execution, durable resume, and cancellation. A registered application may wrap the complete neutral attempt route, but cannot replace backend selection, Session authoring, WorkQueue placement, or realization through another Worker/Host path. Each durable attempt receives a claim-bound ownership verifier; current ownership includes the exact epoch and a live lease, while server-side Worker/Run checks use the same `DispatchQueue` authority and receive its exact current claim only for the authenticated incarnation and a non-cancelled Run. | `SessionAttemptExecutor` over `AttemptExecutorRegistry`; `RegisteredWorkerContext` plus the attempt decorator; `RuntimeRunContext::ownership`; `DispatchQueue::claim_is_current`/`worker_owns_run`; A2A Run-scoped `__a2a_task` state | Worker registration-order test; frozen-Session realization tests; cross-backend current/expired/stale/cancelled ownership conformance; signed HTTP claim test; backend-router execute/resume/cancel and A2A recovery/cancellation conformance (ADR-0065, ADR-0075) |
| G41 | A delegated local child is an ordinary Run, not a second execution aggregate. Parentage lives only in `DelegationOrigin`/`DelegationRegistry`; durable delivery uses `RunDispatch`, and `session_thread_id` supplies parent-Session environment affinity without replacing the child's own Run/thread identity. Auxiliary Agent helpers cannot accept a child id or delegation origin. | ordinary `RunActivation` + `RunAttemptExecutor`; `RunDispatch::for_session`; `run_configured_agent_until_boundary`; auxiliary-only `run_configured_agent` | runtime explicit-identity parity; durable child scheduler/HITL tests; hard-crash TS E2E proves stable child id, same sandbox handle, one seed/result, and parent completion; architecture/API review finds no child executor/store or delegated completion helper |
| G44 | Provider connection has one application command shared by HTTP and embedded hosts: a direct endpoint identity defaults to `(provider_id, dialect)` and accepts an explicit name only to qualify another endpoint on the same dialect; descriptor-owned inputs are normalized server-side, credential entry is idempotent by an explicit command key, discovery precedes persistence, Catalog reconciliation precedes activation, and executable model readiness is one server-owned Catalog/access/executor projection. Direct/BYOK Offerings require a compatible active Workspace Credential; Brokered Offerings require the existing brokered deployment capability and never a copied local provider key. Transports and clients may map values but may not author endpoint ids or duplicate orchestration, provider endpoint rules, credential creation, or readiness joins. | `ProtocolEndpointId::for_surface`; `ProviderConnectionService`; `enter_credential_idempotent`; `project_executable_models`; `ModelSupplyCapabilityView::cloud_models_enabled`; `GenaiModelDiscovery`; generated OpenAPI/TypeScript contract | provider/dialect/name surface-identity decision table; provider command identity decision table; staged-credential/catalog-failure test; Vertex configuration test; direct/brokered executable-model decision table; web tests contain no provider endpoint/readiness join implementation |
| G45 | Control owns immutable Agent publications and Environment revisions/policy references; Coordinator owns rebuildable executable projections plus the sole Deployment/DeploymentRun/Session/WorkQueue application path; Resources owns one application path over its catalog, File/Memory/Skill stores, and lifecycle repository; Worker executes only through authenticated, claim-fenced services. AllInOne starts those same domain applications, and no whole-catalog install, Session-owned Environment registry, Host-side File command path, remote Deployment-launch path, standalone Resources role without claim/reference transport, or universal Resource materializer exists beside them. | `StaticRegistrationSupervisor` over the existing Agent/Environment recovery services; `ExecutableAgentRegistrar`/`ExecutableAgentCatalog`; executable-Environment registration/catalog; shared durable `ProjectionCursor`; `ResourceAuthorities`, `ResourcesApplication`, `FileApplicationService`, and one Resources router; Coordinator `DeploymentApplication` plus `ManagedDeploymentSessionLauncher`; authority-store-free `awaken-worker`; role-scoped migration manifests | registration readiness/backoff/lag decision tests; incremental/high-water/full-replay projection tests; Environment history/withdrawal, resource application, resource-route, role-surface, local launch, and per-kind cause/effect tests; migration lint and restart tests; Worker and premature Resources-role rejection; service-data-ownership rules in `scripts/ci/check_crate_boundaries.py` |
| G46 | Model-supply deployment posture is one server-enforced capability contract. Hosted supply may project brokered Provider-native models for selection but cannot author a local Provider connection, model route, Provider-scoped credential, or inference Profile; publication remains owned by the injected `ModelPublicationResolver`, and generic non-model credentials remain independent. | `ModelSupplyCapabilityView`; admin command gates; product-owned `model_supply.*` actions; `BrokeredCatalogDiscovery` projection plus `ModelPublicationResolver` publication authority | capability/command cause-effect decision table; hosted router API tests; authorization-profile and route-classification tests; browser UI tests |
| G47 | A Session has two orthogonal durable state axes: `SessionExecutionState` owns preparation, activity, failure, and termination; `SessionDisposition` owns active, archived, deleting, and deleted visibility. `PersistedSession` is their only authority, `SessionApplication` is their only command owner, and every mutation shares the root revision CAS. `SessionDeleteCommand` is the protocol-neutral application command and the application alone derives the canonical `session.deleted` outbox fact. A realization acknowledgement cannot settle an overlapping `Running` activity; terminal realization failure closes that activity's aggregate-owned runtime interval and emits its existing lifecycle fact in the same root CAS. Closed runtime intervals, exact processed Event-entry anchors, and the terminal-cleanup quiescent Runtime high-water remain in that root for the Session lifetime; Runtime message/lifecycle/state commit cursors order their immutable projection without creating another event store. Unanchored new facts are withheld, not guessed. Stores may compatibility-decode historical `status`/`archived_at`; protocols may only derive wire status and archive time. First write of new aggregate JSON fields requires a full-stop old-writer maintenance cutover; defaults alone do not make mixed binaries compatible. Thread/Run `RunState`, Host slots, leases, and physical cleanup remain facts of their own bounded contexts. | `PersistedSession::transition_execution`/`observe_runtime_interval`/`close_runtime_interval`/`archive`/`request_delete`; `SessionEventBatch::mark_processed`; `SessionCleanupOperation::freeze_targets`; `SessionDeleteCommand`; `SessionApplication`; `ManagedSessionRepository`; Session-state ownership fitness in `scripts/ci/check_crate_boundaries.py` | transition, disposition, realization/activity-overlap, late-fact prefix stability, two-turn warm/cold every-cursor, and mixed-writer release decision tables; SQLite/Postgres aggregate-required, index-cross-validation, quarantine, and root-CAS conformance; archive/delete/failure restart tests; protocol projection tests (ADR-0075) |
| G48 | Session external effects use domain-owned typed protocols, not a universal effect aggregate: Environment create/adopt, MCP publish/drain, terminal cleanup, explicit terminal Repository publication, and artifact harvest each commit or derive a stable command, execute idempotently behind the current fence, verify an exact completion, and settle durable truth through the same recovery entry. `SessionCleanupOperation` first persists `Fenced`, cancels and settles the parent dispatch, retires Session Work, then freezes root plus every child identity from durable `RunDelegations` with its committed-state watermark; protocol projections can never supply or expand cleanup targets. When the fence carries one explicit writable Repository intent, the immutable cleanup operation projects child cleanup first, then one root-only publication of the frozen `ResolvedInput` to its exact branch and full commit, commits the canonical publication receipt through the Session root CAS, and only then exposes root cleanup that may dispose the shared working tree. The same `RepositoryRealizer` handles clone and publication; source identity stays distinct from an authorized transport URL, and no credential material or Gateway capability is persisted. An untrusted `SessionCleanupCompletion` becomes a non-serializable `VerifiedSessionCleanupReceipt` only after exact command binding and full Repository/Skill/artifact/Environment settlement. Cleanup without publication retains its exact v1 wire and fingerprints. SQLite and PostgreSQL independently recheck hidden disposition, terminal execution, verified cleanup, exact Session identity, and exact next revision inside the deletion transaction before replacing the aggregate with a tombstone. | `SessionEnvironmentReceipt`; `McpProjectionReceipt`; `SessionCleanupOperation`/`SessionCleanupCommand`/`SessionCleanupCompletion`/`VerifiedSessionCleanupReceipt`; `SessionRepositoryPublicationIntent`/`SessionRepositoryPublicationCommand`/`SessionRepositoryPublicationReceipt`; `RepositoryPublicationExpectation`/`RepositoryPublicationReceipt`; `RepositoryRealizer`; `PersistedSession::admits_tombstone`; `DelegatedRunSnapshot`; `ArtifactPublication`/receipt; shared realization and terminal cleanup drivers; Session-effect fitness in `scripts/ci/check_crate_boundaries.py` | Kani cleanup phase/completion/tombstone proofs; `SessionDeletion.tla`; store conformance rejection/no-mutation matrix; v1 no-publication JSON/fingerprint compatibility; child-first publication/root-gate decision table; exact first/replay/conflicting-ref Git tests; crash-before-completion retry; completed-replay no-effect test; deterministic archive/delegation race and child-thread restart tests; claim-fenced artifact and Repository transport tests; workspace compile/test gates |
| G49 | Signed database integers representing monotonic runtime authority are decoded and encoded only through one checked persistence codec: writes reject values above `i64::MAX`, reads reject negatives, and arithmetic rejects overflow. Durable schemas reject negative sequence/version/ordinal values. | `StoredU64` in `awaken-store-schema`; SQLite triggers and PostgreSQL checks; Commit/Recovery adapters depend on the codec | shared `-1/0/i64::MAX/i64::MAX+1` codec tests; SQLite/PostgreSQL migration tests; metadata-discovered authority arithmetic fitness |
| G50 | Session repository absence, outage, corruption, conflict, and invalid mutation are distinct closed results. Every persistence operation returns `Result`; only protocol boundaries map `NotFound` to 404. Recovery classifies `Unavailable` for retry and `Corrupt` for quarantine/alert, and no repository error may degrade to `None`, an empty scan, or a process panic. | `SessionRepositoryError` and `SessionRepositoryRecoveryAction`; `ManagedSessionRepository`; Session application/protocol error adapters | repository conformance and corrupt-row tests; unavailable scan tests; Session-effect fitness rejects compatibility wrappers and panic serialization |
| G51 | A migration has one version/slug identity and either one portable SQL body or an exact PostgreSQL/SQLite pair owned by `Migration::per_dialect`; runtime bundling and CI validate the same naming and pairing model. Published migration bodies remain immutable. | migration bundle declarations plus `_migration_fitness.py` | migration selftest; architecture gate; SQLite/PostgreSQL apply and historical-checksum tests |
| G52 | The production Sandbox image has one Awaken-owned build and release path. An exact protected `vX.Y.Z` tag in `awakenworks/awaken` selects one revision. The sole workflow first resolves the semantic tag before any build: an existing tag is reusable only when its immutable digest has exact OCI labels, an exact current-repository/workflow-SHA/ref/push-trigger keyless image signature, and exactly one verified strict Open predicate; absent, conflicting, or multiple evidence fails without completing or mutating that tag. If the tag is absent, the existing Sandbox build/acceptance owner pushes a run-scoped staging tag, resolves and labels-checks its immutable digest, keyless-signs, attests, and verifies it, and only then promotes that same manifest after an exact write-before/write-after tag comparison. Consumers retain the strict exactly-one validator and canonical digest, and cannot build, republish, or interpret a parallel schema. Branch, pull-request, manual, `latest`, private-key, mutable-action, release-tag build/push, local-script, second-workflow, extra release tag, pre-proof promotion, and unsigned-tag completion are forbidden. | `deploy/images/sandbox/build.sh`; `.github/workflows/release.yml`; `scripts/release/resolve_awaken_sandbox_release_image.sh`; `scripts/release/awaken_sandbox_image_provenance.py`; `scripts/ci/check_sandbox_image_release.py` | predicate and release-metadata cause/effect self-test; executable registry-resolver decision table; repository-wide writer inventory; workflow/static prove-before-promotion decision table; build-label self-test; documentation and repository-hygiene gates (ADR-0041 Amendment 5) |

## Target Guardrails

The following rules are accepted architecture targets, not current repository
invariants. They move into the enforced table above only after their authoritative
types replace the old paths and the listed evidence is green.

| ID | Target rule | Planned enforcement | Promotion evidence |
|---|---|---|---|
| G42 | A scoped persistence envelope carries opaque ownership beside, never inside, the Session aggregate. A Session has one root revision, one consumed complete `SessionCreationIntent`, one immutable finalized `SessionBaseline` with one closed `SessionMutationPolicy` and one typed `SessionSystemPromptSelection`, the existing versioned `SessionResourceState`, and one versioned `SessionMcpAttachmentSet`. Historical and ordinary Managed Sessions retain the existing authoring surface; profiled WorkUnit/Interactive creation freezes `Frozen`/`FileResources` before insertion. Non-Managed public Agent and Repository-credential authoring fails closed; `Frozen` rejects Resource replacement, while `FileResources` admits only File-binding changes that preserve every non-File input and exact Skill pin. Typed Vault credential lifecycle adoption may rotate the same source monotonically or revoke only its affected MCP attachments, never widen topology. The root repository atomically classifies create as `Applied` or `Replayed`; only `Applied` may realize, activate, or dispatch, while exact replay returns durable truth and an `ActivationFailed` root remains a typed terminal conflict. Create receipts name revision 1, mutation receipts name their exact expected next revision, and dangling/ahead/double identities fail as corrupt. Agent and Session MCP inputs share one Control normalizer and attachment authority. A frozen nonterminal self-hosted Session always projects through the Environment WorkQueue; Worker Control may realize committed truth but cannot author it. External realization is fenced by `SessionRealizationLease`/per-generation claims and follows durable Requested→Realizing→Active→Draining→Removed; only post-CAS Active generations are published. | atomic create/replace/tombstone over `ManagedSessionRepository`; `SessionCreateResult` and `replay_create`; `SessionMutationPolicy`; `SessionSystemPromptSelection`; `SessionMcpUpdate`; one Managed `McpAttachmentNormalizer`; canonical `ResolvedSessionResources` comparison; Environment WorkQueue; claim-fenced frozen-Session resume; one `McpAttachmentRealizer` plus shared `drive_session_realization`; generation-aware relay/descriptor/call identity | policy and prompt-selection serialization/fingerprint decision tables; complete-create Applied/Replayed/no-effect and receipt-corruption tables; public-Agent, credential-lifecycle, Resource-manifest, File-only, Repository-credential, and private-whole-manifest rejection tests; SQLite/Postgres root-CAS/delete-replay/idempotency/outbox conformance; WorkQueue idempotency; stale-claim and Worker replacement/lease tests; Resource/MCP/environment conflicts; ordinary SDK full-replacement E2E; stale-generation and out-of-policy network tests (ADR-0066, ADR-0075) |
| G43 | Credential storage, material source, sealed payload reference, target usage, exact OAuth refresh/reseal access, allowed plaintext holders, model exposure, planned realization, actual receipt, and authorization are distinct facts. Published access carries one exact credential reference, source/envelope reference, existing `CredentialUsage`, an explicit nonempty set of allowed `PlaintextHolder` trust domains, and `Forbidden` or `VirtualOnly` exposure. An Environment/execution profile requests purpose-specific exact holders; MCP and Repository Resource generations or the atomic dispatch claim-epoch binding pin them before materialization. Claim admission decodes the contract-owned frozen Session runtime projection and admits each MCP pin against the selected Worker's installed capabilities through the same `CredentialAccess::admit` kernel before effects. A hosted issuer may seal only the already-authorized exact Vault revision and binding for that selected holder; it cannot select, expose plaintext over HTTP, or fall back to an unsealed reference. Repository realization uses the same holder selection: Worker means direct self-hosted injection, while Platform means claim-fenced Gateway mediation whose short capability is injected into the unchanged Git transport and whose upstream material is opened only inside the Gateway. There is no custody order, runtime holder search/fallback, bare Repository source materialization, URL/current-Vault refresh rediscovery, inference Service attachment, or Session model-credential override. | amended `CredentialAccess`; exact `CredentialMaterialResolver`; optional deployment `CredentialEnvelopeIssuer` at the canonical Vault boundary; source/envelope migration from `CredentialInjectionKind`; claim-epoch `AttemptCredentialBinding`, secret-free realization receipt, MCP-generation holder pin, Repository exact access/holder pin and direct-or-mediated transport, and exact `CredentialRefreshAccess`; `SessionRuntimeEnvelope`/`DispatchedSessionRuntimeProjection`; `ResolvedModelCandidate` remains sole Model authority | Slice 0 contract review; access/envelope/ref resolver serialization/fingerprint/recipient/replay tests; exact Vault/Workspace issuer admission; atomic claim/retry/reclaim pins; Session MCP claim-admission decision table; Repository anonymous/exact/missing/mismatched/stale/inactive/cross-Workspace decision table, direct/mediated/no-fallback transport table and no-secret proof; exact OAuth public/confidential refresh+reseal+restart tests; ACP typed last-mile delivery; Worker relay substitution plus no-bypass proof; prompt/tool/resource leak scans; Native/ACP exact-candidate parity and no-downgrade TypeScript E2E (ADR-0067) |

## DDD Review Checklist

Before implementing a design, answer these questions:

1. Which bounded context owns the behavior?
2. Is this an aggregate, entity, value object, domain service, repository, or
   adapter?
3. Which existing port carries the dependency?
4. What invariant prevents the main failure mode, and what is its enforcer?
5. What is the smallest vertical slice that exercises the behavior end to end?
6. Which names must not cross the boundary?

If the answer requires a new facade, crate, or role word, prefer using the
existing domain vocabulary (ADR-0001 D3) unless the new concept has a distinct
invariant and test.
